Place your background music file here as:  birthday-theme.mp3

Recommended: a soft, emotional instrumental track (piano/strings) around 2-4 minutes,
looping quietly in the background. Keep the file under ~5MB for fast loading on mobile.

The site references it at: audio/birthday-theme.mp3
(configured in js/config.js -> SITE_CONFIG.music.src)

Until a real file is added, the music toggle button will simply do nothing when clicked
(fails silently) — it will NOT break the site or throw visible errors.
